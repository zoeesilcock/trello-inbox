<%= simple_form_for @inbox do |f| %>
  <%= f.input :title %>
  <%= f.input :board_id, collection: @boards %>
  <%= f.input :description %>
  <%= f.submit @inbox.persisted? ? t('.update') : t('.create'), class: 'btn btn-primary pull-right' %>
<% end %>
