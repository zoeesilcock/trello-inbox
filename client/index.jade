doctype html
html

  head
    title Hello, React

  body

    h1.alert.alert-info.this-works webpack dev server (with HMR)
    #app

    script(src="vendor-bundle.js")
    script(src="ideas-bundle.js")

    script.
      ReactDOM.render(React.createElement(IdeasContainer, { ideas: [] }), document.getElementById('app'))
